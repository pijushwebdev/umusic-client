import useCart from "../../../hooks/useCart";



const SelectedClass = () => {
    const [cart] = useCart();


    return (
        <div>
            
        </div>
    );
};

export default SelectedClass;